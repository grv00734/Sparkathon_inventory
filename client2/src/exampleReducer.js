const initialState = {
  message: 'Hello from Redux',
};

const exampleReducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default exampleReducer;

